/** Nudge step in mm for arrow key movement */
export const NUDGE_STEP = 10

/** Quarter turn in radians (90°) */
export const QUARTER_TURN = Math.PI / 2

/** Full rotation in radians (360°) */
export const TAU = Math.PI * 2

/** Keyboard: elements that should not trigger shortcuts */
export const TYPING_SELECTOR = 'input, textarea, [contenteditable="true"]'
